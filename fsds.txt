print("Loading neverlose")
print("-----------------------------------------")
local Library = loadstring(game:HttpGetAsync("https://github.com/1dontgiveaf/Fluent-Renewed/releases/download/v1.0/Fluent.luau"))()
local SaveManager = loadstring(game:HttpGetAsync("https://raw.githubusercontent.com/1dontgiveaf/Fluent-Renewed/refs/heads/main/Addons/SaveManager.luau"))()
local InterfaceManager = loadstring(game:HttpGetAsync("https://raw.githubusercontent.com/1dontgiveaf/Fluent-Renewed/refs/heads/main/Addons/InterfaceManager.luau"))()

local Window = Library:CreateWindow{
    Title = "neverlose",
    SubTitle = ":)",
    TabWidth = 160,
    Size = UDim2.fromOffset(830, 525),
    Resize = true,
    MinSize = Vector2.new(470, 380),
    Acrylic = true,
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.LeftControl
}

local Tabs = {
    Main = Window:AddTab({ Title = "Main", Icon = "menu" }),
    Map = Window:AddTab({ Title = "Map", Icon = "trees" }),
    Pickup = Window:AddTab({ Title = "Pickup", Icon = "backpack" }),
    Farming = Window:AddTab({ Title = "Farming", Icon = "sprout" }),
    Event = Window:AddTab({ Title = "Event", Icon = "zap" }),
    Settings = Window:AddTab({ Title = "Settings", Icon = "settings" })
}

local rs = game:GetService("ReplicatedStorage")
local packets = require(rs.Modules.Packets)
local plr = game.Players.LocalPlayer
local GameUtil = require(rs.Modules.GameUtil)
local clock = require(rs.Modules.Clock)
-- Initialize char, root, hum initially
local char, root, hum

local function updateCharacter()
    char = plr.Character
    if not char then return end
    root = char:WaitForChild("HumanoidRootPart", 5) -- Wait max 5 seconds
    hum = char:WaitForChild("Humanoid", 5)
end

updateCharacter()
plr.CharacterAdded:Connect(updateCharacter)

local runs = game:GetService("RunService")
local httpservice = game:GetService("HttpService")
local Players = game:GetService("Players")
local localiservice = game:GetService("LocalizationService")
local marketservice = game:GetService("MarketplaceService")
local rbxservice = game:GetService("RbxAnalyticsService")
local placestructure
local tspmo = game:GetService("TweenService")
local itemslist = {
"Adurite", "Berry", "Bloodfruit", "Bluefruit", "Coin", "Essence", "Hide", "Ice Cube", "Iron", "Jelly", "Leaves", "Log", "Steel", "Stone", "Wood", "Gold", "Raw Gold", "Crystal Chunk", "Raw Emerald", "Pink Diamond", "Raw Adurite", "Raw Iron", "Coal"}
local Options = Library.Options

local wstoggle = Tabs.Main:CreateToggle("wstoggle", { Title = "Walkspeed", Default = false })
local wsslider = Tabs.Main:CreateSlider("wsslider", { Title = "Value", Min = 1, Max = 35, Rounding = 1, Default = 16 })
local jptoggle = Tabs.Main:CreateToggle("jptoggle", { Title = "JumpPower", Default = false })
local jpslider = Tabs.Main:CreateSlider("jpslider", { Title = "Value", Min = 1, Max = 65, Rounding = 1, Default = 50 })
local msatoggle = Tabs.Main:CreateToggle("msatoggle", { Title = "No Mountain Slip", Default = false })

local killauratoggle -- removed combat; placeholder to avoid nil indexing if referenced elsewhere

-- FIXED: Resource Aura with proper connections and GameUtil check
local resourceauratoggle = Tabs.Map:CreateToggle("resourceauratoggle", { Title = "Resource Aura", Default = false })
local resourceaurarange = Tabs.Map:CreateSlider("resourceaurarange", { Title = "Range", Min = 1, Max = 20, Rounding = 1, Default = 20 })
local resourcetargetdropdown = Tabs.Map:CreateDropdown("resourcetargetdropdown", { Title = "Max Targets", Values = { "1", "2", "3", "4", "5", "6" }, Default = "1" })
local resourcecooldownslider = Tabs.Map:CreateSlider("resourcecooldownslider", { Title = "Swing Cooldown (s)", Min = 0.01, Max = 1.01, Rounding = 2, Default = 0.1 })

-- FIXED: Critter Aura with proper connections and GameUtil check
local critterauratoggle = Tabs.Map:CreateToggle("critterauratoggle", { Title = "Critter Aura", Default = false })
local critterrangeslider = Tabs.Map:CreateSlider("critterrangeslider", { Title = "Range", Min = 1, Max = 20, Rounding = 1, Default = 20 })
local crittertargetdropdown = Tabs.Map:CreateDropdown("crittertargetdropdown", { Title = "Max Targets", Values = { "1", "2", "3", "4", "5", "6" }, Default = "1" })
local crittercooldownslider = Tabs.Map:CreateSlider("crittercooldownslider", { Title = "Swing Cooldown (s)", Min = 0.01, Max = 1.01, Rounding = 2, Default = 0.1 })

local autopickuptoggle = Tabs.Pickup:CreateToggle("autopickuptoggle", { Title = "Auto Pickup", Default = false })
local chestpickuptoggle = Tabs.Pickup:CreateToggle("chestpickuptoggle", { Title = "Auto Pickup From Chests", Default = false })
local pickuprangeslider = Tabs.Pickup:CreateSlider("pickuprange", { Title = "Pickup Range", Min = 1, Max = 35, Rounding = 1, Default = 20 })

-- UPDATED: Added "Bread", "Cooked Egg", "Grain Mush" to the Values list
local itemdropdown = Tabs.Pickup:CreateDropdown("itemdropdown", {
    Title = "Items", 
    Values = {
        "Berry", "Bloodfruit", "Bluefruit", "Lemon", "Strawberry", "Gold", "Raw Gold", 
        "Crystal Chunk", "Coin", "Coins", "Coin2", "Coin Stack", "Essence", "Emerald", 
        "Raw Emerald", "Pink Diamond", "Raw Pink Diamond", "Void Shard","Jelly", 
        "Magnetite", "Raw Magnetite", "Adurite", "Raw Adurite", "Ice Cube", "Stone", 
        "Iron", "Raw Iron", "Steel", "Hide", "Leaves", "Log", "Wood", "Glass", 
        "Bread", "Cooked Egg", "Grain Mush", "Raw Meat", "Cooked Meat", "Glass Bottle", "Healing Potion", "Strength Potion", "Swiftness Potion", -- NEW ITEMS ADDED HERE
    }, 
    Multi = true, 
    Default = { Leaves = true, Log = true }
})

local droptoggle = Tabs.Pickup:AddToggle("droptoggle", { Title = "Auto Drop", Default = false })
local dropdropdown = Tabs.Pickup:AddDropdown("dropdropdown", {Title = "Select Item to Drop", Values = { "Bloodfruit", "Jelly", "Bluefruit", "Log", "Leaves", "Wood" }, Default = "Bloodfruit"})
local droptogglemanual = Tabs.Pickup:AddToggle("droptogglemanual", { Title = "Auto Drop Custom", Default = false })
local droptextbox = Tabs.Pickup:AddInput("droptextbox", { Title = "Custom Item", Default = "Bloodfruit", Numeric = false, Finished = false })

local fruitdropdown = Tabs.Farming:CreateDropdown("fruitdropdown", {Title = "Select Fruit",Values = {"Bloodfruit", "Bluefruit", "Lemon", "Coconut", "Jelly", "Banana", "Orange", "Oddberry", "Berry", "Strangefruit", "Strawberry", "Sunjfruit", "Pumpkin", "Prickly Pear", "Apple",  "Barley", "Cloudberry", "Carrot", "Blossom"}, Default = "Bloodfruit"}) 
local planttoggle = Tabs.Farming:CreateToggle("planttoggle", { Title = "Auto Plant", Default = false })
local plantrangeslider = Tabs.Farming:CreateSlider("plantrange", { Title = "Plant Range", Min = 1, Max = 100, Rounding = 1, Default = 30 }) 
local plantdelayslider = Tabs.Farming:CreateSlider("plantdelay", { Title = "Plant Delay (s)", Min = 0.01, Max = 1, Rounding = 2, Default = 0.1 }) 
local harvesttoggle = Tabs.Farming:CreateToggle("harvesttoggle", { Title = "Auto Harvest", Default = false })
local harvestrangeslider = Tabs.Farming:CreateSlider("harvestrange", { Title = "Harvest Range", Min = 1, Max = 30, Rounding = 1, Default = 30 })

-- NEW: Tween Speed Slider
local tweenspeedslider = Tabs.Farming:AddSlider("tweenspeed", { 
    Title = "Tween Speed (Higher = Faster)", 
    Min = 10, 
    Max = 200, 
    Rounding = 1, 
    Default = 50 
})

local tweenplantboxtoggle = Tabs.Farming:AddToggle("tweentoplantbox", { Title = "Tween to Plant Box", Default = false })
local tweenbushtoggle = Tabs.Farming:AddToggle("tweentobush", { Title = "Tween to Bush + Plant Box", Default = false })
local tweenrangeslider = Tabs.Farming:AddSlider("tweenrange", { Title = "Range", Min = 1, Max = 250, Rounding = 1, Default = 250 })

-- NEW: Continuous Plant Box Placer Toggle
local placeplantboxestoggle = Tabs.Farming:AddToggle("placeplantboxestoggle", { Title = "Place Plant Boxes Continuously", Default = false })

-- NEW: Plant Box Rotation Dropdown
local rotationdropdown = Tabs.Farming:AddDropdown("rotationdropdown", {Title = "Plant Box Rotation", Values = {"0 degrees", "45 degrees"}, Default = "0 degrees"})

-- Button to trigger single placement (optional, can be removed if only using toggle)
Tabs.Farming:CreateButton({Title = "Place Single Plant Box (Snapped)", Callback = function() placestructure(1) end }) 

Tabs.Farming:CreateButton({Title = "Place 16x16 Plantboxes (256)", Callback = function() placestructure(16) end })
Tabs.Farming:CreateButton({Title = "Place 15x15 Plantboxes (225)", Callback = function() placestructure(15) end })
Tabs.Farming:CreateButton({Title = "Place 10x10 Plantboxes (100)", Callback = function() placestructure(10) end })
Tabs.Farming:CreateButton({Title = "Place 5x5 Plantboxes (25)", Callback = function() placestructure(5) end })

local wscon
local function updws()
    if wscon then wscon:Disconnect() end
    if Options.wstoggle.Value or Options.jptoggle.Value then
        wscon = runs.RenderStepped:Connect(function()
            if hum then
                hum.WalkSpeed = Options.wstoggle.Value and Options.wsslider.Value or 16
                hum.JumpPower = Options.jptoggle.Value and Options.jpslider.Value or 50
            end
        end)
    end
end

local slopecon
local function updmsa()
    if slopecon then slopecon:Disconnect() end
    if Options.msatoggle.Value then
        slopecon = game:GetService("RunService").RenderStepped:Connect(function()
            if hum then
                hum.MaxSlopeAngle = 90
            end
        end)
    else
        if hum then
            hum.MaxSlopeAngle = 46
        end
    end
end

-- Note: Character update is handled by updateCharacter function connected earlier
Options.wstoggle:OnChanged(updws)
Options.jptoggle:OnChanged(updws)

Options.msatoggle:OnChanged(updmsa)

local function getlayout(itemname)
    local inventory = game:GetService("Players").LocalPlayer.PlayerGui.MainGui.RightPanel.Inventory:FindFirstChild("List")
    if not inventory then
        return nil
    end
    for _, child in ipairs(inventory:GetChildren()) do
        if child:IsA("ImageLabel") and child.Name == itemname then
            return child.LayoutOrder
        end
    end
    return nil
end

-- FIXED: SwingTool now uses correct packet structure with GameUtil and clock
local function swingtool(entityIds)
    if packets.SwingTool and packets.SwingTool.send then
        -- Build entityIDs array with correct structure
        local entityIDsArray = {}
        for _, eid in ipairs(entityIds) do
            table.insert(entityIDsArray, {
                entityID = eid,
                buffer = 0
            })
        end
        
        -- Send SwingTool packet with correct structure using clock.getServerTime
        pcall(function()
            packets.SwingTool.send({
                entityIDs = entityIDsArray,
                cframe = root and root.CFrame or CFrame.new(),
                timestamp = clock.getServerTime(true)
            })
        end)
    end
end

local function pickup(entityid)
    if packets.Pickup and packets.Pickup.send then
        packets.Pickup.send(entityid)
    end
end

local function drop(itemname)
    local inventory = game:GetService("Players").LocalPlayer.PlayerGui.MainGui.RightPanel.Inventory:FindFirstChild("List")
    if not inventory then return end
    for _, child in ipairs(inventory:GetChildren()) do
        if child:IsA("ImageLabel") and child.Name == itemname then
            if packets and packets.DropBagItem and packets.DropBagItem.send then
                packets.DropBagItem.send(child.LayoutOrder)
            end
        end
    end
end

local selecteditems = {}
itemdropdown:OnChanged(function(Value)
    selecteditems = {} 
    for item, State in pairs(Value) do
        if State then
            table.insert(selecteditems, item)
        end
    end
end)

-- FIXED: Resource Aura with proper GameUtil.Data.equipped check
local resourceAuraActive = false
local resourceAuraConnection = nil
local lastResourceSwingTime = 0

local function StartResourceAura()
    if resourceAuraActive then return end
    resourceAuraActive = true
    
    resourceAuraConnection = runs.Heartbeat:Connect(function()
        if not resourceauratoggle.Value then
            if resourceAuraConnection then
                resourceAuraConnection:Disconnect()
                resourceAuraConnection = nil
            end
            resourceAuraActive = false
            return
        end
        
        if not root then return end
        
        -- Check if player has a tool equipped
        if not GameUtil.Data.equipped then return end
        
        local currentTime = tick()
        local cooldown = tonumber(Options.resourcecooldownslider.Value) or 0.1
        if currentTime - lastResourceSwingTime < cooldown then return end
        
        local range = tonumber(Options.resourceaurarange.Value) or 20
        local targetCount = tonumber(Options.resourcetargetdropdown.Value) or 1
        
        local targets = {}
        local allresources = {}
        
        for _, r in pairs(workspace.Resources:GetChildren()) do
            table.insert(allresources, r)
        end
        for _, r in pairs(workspace:GetChildren()) do
            if r:IsA("Model") and r.Name == "Gold Node" then
                table.insert(allresources, r)
            end
        end
        
        for _, res in pairs(allresources) do
            if res:IsA("Model") and res:GetAttribute("EntityID") then
                local eid = res:GetAttribute("EntityID")
                local ppart = res.PrimaryPart or res:FindFirstChildWhichIsA("BasePart")
                if ppart then
                    local dist = (ppart.Position - root.Position).Magnitude
                    if dist <= range then
                        table.insert(targets, { eid = eid, dist = dist })
                    end
                end
            end
        end
        
        if #targets > 0 then
            table.sort(targets, function(a, b)
                return a.dist < b.dist
            end)
            local selectedTargets = {}
            for i = 1, math.min(targetCount, #targets) do
                table.insert(selectedTargets, targets[i].eid)
            end
            swingtool(selectedTargets)
            lastResourceSwingTime = currentTime
        end
    end)
end

local function StopResourceAura()
    resourceAuraActive = false
    if resourceAuraConnection then
        resourceAuraConnection:Disconnect()
        resourceAuraConnection = nil
    end
end

-- Connect the toggle to the fixed functions
resourceauratoggle:OnChanged(function(state)
    if state then
        StartResourceAura()
    else
        StopResourceAura()
    end
end)

-- FIXED: Critter Aura with proper GameUtil.Data.equipped check
local critterAuraActive = false
local critterAuraConnection = nil
local lastCritterSwingTime = 0

local function StartCritterAura()
    if critterAuraActive then return end
    critterAuraActive = true
    
    critterAuraConnection = runs.Heartbeat:Connect(function()
        if not critterauratoggle.Value then
            if critterAuraConnection then
                critterAuraConnection:Disconnect()
                critterAuraConnection = nil
            end
            critterAuraActive = false
            return
        end
        
        if not root then return end
        
        -- Check if player has a tool equipped
        if not GameUtil.Data.equipped then return end
        
        local currentTime = tick()
        local cooldown = tonumber(Options.crittercooldownslider.Value) or 0.1
        if currentTime - lastCritterSwingTime < cooldown then return end
        
        local range = tonumber(Options.critterrangeslider.Value) or 20
        local targetCount = tonumber(Options.crittertargetdropdown.Value) or 1
        
        local targets = {}
        
        -- Check if workspace.Critters exists
        local crittersFolder = workspace:FindFirstChild("Critters")
        if crittersFolder then
            for _, critter in pairs(crittersFolder:GetChildren()) do
                if critter:IsA("Model") and critter:GetAttribute("EntityID") then
                    local eid = critter:GetAttribute("EntityID")
                    local ppart = critter.PrimaryPart or critter:FindFirstChildWhichIsA("BasePart")
                    if ppart then
                        local dist = (ppart.Position - root.Position).Magnitude
                        if dist <= range then
                            table.insert(targets, { eid = eid, dist = dist })
                        end
                    end
                end
            end
        end
        
        if #targets > 0 then
            table.sort(targets, function(a, b)
                return a.dist < b.dist
            end)
            local selectedTargets = {}
            for i = 1, math.min(targetCount, #targets) do
                table.insert(selectedTargets, targets[i].eid)
            end
            swingtool(selectedTargets)
            lastCritterSwingTime = currentTime
        end
    end)
end

local function StopCritterAura()
    critterAuraActive = false
    if critterAuraConnection then
        critterAuraConnection:Disconnect()
        critterAuraConnection = nil
    end
end

-- Connect the toggle to the fixed functions
critterauratoggle:OnChanged(function(state)
    if state then
        StartCritterAura()
    else
        StopCritterAura()
    end
end)

-- Keep the original pickup loop but remove the old aura loops that were replaced
task.spawn(function()
    while true do
        local range = tonumber(Options.pickuprange.Value) or 35
        if Options.autopickuptoggle.Value then
            for _, item in ipairs(workspace.Items:GetChildren()) do
                if item:IsA("BasePart") or item:IsA("MeshPart") then
                    local selecteditem = item.Name
                    local entityid = item:GetAttribute("EntityID")
                    if entityid and table.find(selecteditems, selecteditem) then
                        local dist = (item.Position - root.Position).Magnitude
                        if dist <= range then
                            pickup(entityid)
                        end
                    end
                end
            end
        end
        if Options.chestpickuptoggle.Value then
            for _, chest in ipairs(workspace.Deployables:GetChildren()) do
                if chest:IsA("Model") and chest:FindFirstChild("Contents") then
                    for _, item in ipairs(chest.Contents:GetChildren()) do
                        if item:IsA("BasePart") or item:IsA("MeshPart") then
                            local selecteditem = item.Name
                            local entityid = item:GetAttribute("EntityID")
                            if entityid and table.find(selecteditems, selecteditem) then
                                local dist = (chest.PrimaryPart.Position - root.Position).Magnitude
                                if dist <= range then
                                    pickup(entityid)
                                end
                            end
                        end
                    end
                end
            end
        end
        task.wait(0.01)
    end
end)

local debounce = 0
local cd = 0
runs.Heartbeat:Connect(function()
    if Options.droptoggle.Value then
        if tick() - debounce >= cd then
            local selectedItem = Options.dropdropdown.Value
            drop(selectedItem)
            debounce = tick()
        end
    end
end)

runs.Heartbeat:Connect(function()
    if Options.droptogglemanual.Value then
        if tick() - debounce >= cd then
            local itemname = Options.droptextbox.Value
            drop(itemname)
            debounce = tick()
        end
    end
end)

local plantedboxes = {}
local fruittoitemid = {
    Bloodfruit = 94,
    Bluefruit = 377,
    Lemon = 99,
    Coconut = 1,
    Jelly = 604,
    Banana = 606,
    Orange = 602,
    Oddberry = 32,
    Berry = 35,
    Strangefruit = 302,
    Strawberry = 282,
    Sunfruit = 128,
    Pumpkin = 80,
    ["Prickly Pear"] = 378,
    Apple = 243,
    Barley = 247,
    Cloudberry = 101,
    Carrot = 147,
    Blossom = 958 
}

local function plant(entityid, itemID)
    if packets.InteractStructure and packets.InteractStructure.send then
        packets.InteractStructure.send({ entityID = entityid, itemID = itemID })
        plantedboxes[entityid] = true
    end
end

local function getpbs(range)
    local plantboxes = {}
    for _, deployable in ipairs(workspace.Deployables:GetChildren()) do
        if deployable:IsA("Model") and deployable.Name == "Plant Box" then
            local entityid = deployable:GetAttribute("EntityID")
            local ppart = deployable.PrimaryPart or deployable:FindFirstChildWhichIsA("BasePart")
            -- Only add if root exists to prevent 'Position' error
            if entityid and ppart and root then 
                local dist = (ppart.Position - root.Position).Magnitude
                if dist <= range then
                    table.insert(plantboxes, { entityid = entityid, deployable = deployable, dist = dist })
                end
            end
        end
    end
    return plantboxes
end

local function getbushes(range, fruitname)
    local bushes = {}
    for _, model in ipairs(workspace:GetChildren()) do
        if model:IsA("Model") and model.Name:find(fruitname) then
            local ppart = model.PrimaryPart or model:FindFirstChildWhichIsA("BasePart")
            -- Only add if root exists to prevent 'Position' error
            if ppart and root then 
                local dist = (ppart.Position - root.Position).Magnitude
                if dist <= range then
                    local entityid = model:GetAttribute("EntityID")
                    if entityid then
                        table.insert(bushes, { entityid = entityid, model = model, dist = dist })
                    end
                end
            end
        end
    end
    return bushes
end

-- TWEENING SYSTEM WITH SPEED CONTROL
local tweening = nil
local lastPlantTime = 0
local function tween(target)
    if not root then return end -- Check if root exists before tweening
    if tweening then 
        tweening:Cancel()
        tweening = nil
    end
    
    local distance = (root.Position - target.Position).Magnitude
    -- USE SPEED SLIDER: Higher value = Faster movement
    local tweenSpeed = Options.tweenspeed.Value
    local duration = distance / tweenSpeed  -- Speed controlled by slider
    
    -- Safety: Minimum duration to prevent instant teleport at very high speeds
    if duration < 0.1 then duration = 0.1 end
    
    local tweenInfo = TweenInfo.new(duration, Enum.EasingStyle.Linear)
    local tween = tspmo:Create(root, tweenInfo, { CFrame = target })
    tween:Play()
    tweening = tween
    return tween
end

-- IMPROVED: TWEEN TO PLANT BOX WITH SPEED CONTROL AND SMOOTHNESS
local function tweenplantbox(range)
    if not root then task.wait(0.1); return end -- Early return if no root
    -- Get the list of available plant boxes within range that don't have seeds
    local plantboxes = getpbs(range)
    local closestEmptyBox = nil
    local minDist = math.huge

    for _, box in ipairs(plantboxes) do
        if not box.deployable:FindFirstChild("Seed") then
            if box.dist < minDist then
                minDist = box.dist
                closestEmptyBox = box
            end
        end
    end

    if closestEmptyBox then
        local targetCFrame = closestEmptyBox.deployable.PrimaryPart.CFrame + Vector3.new(0, 3, 0) -- Adjust offset as needed
        -- Call the updated tween function which cancels previous tweens
        local currentTween = tween(targetCFrame)
    end
end

-- IMPROVED: TWEEN TO BUSH OR PLANT BOX WITH SPEED CONTROL AND SMOOTHNESS
local function tweenpbs(range, fruitname)
    if not root then task.wait(0.1); return end -- Early return if no root
    -- Get the currently selected fruit from the dropdown inside the loop
    local currentFruitName = Options.fruitdropdown.Value

    -- First, look for harvestable bushes of the specified fruit
    local bushes = getbushes(range, currentFruitName)
    local closestBush = nil
    local minDistBush = math.huge

    for _, bush in ipairs(bushes) do
        if bush.dist < minDistBush then
            minDistBush = bush.dist
            closestBush = bush
        end
    end

    if closestBush then
        -- Found a bush, move towards it
        local targetCFrame = closestBush.model.PrimaryPart.CFrame + Vector3.new(0, 3, 0) -- Adjust offset as needed
        local currentTween = tween(targetCFrame)

        -- Optional: Attempt to harvest if close enough after waiting
        -- Calculate distance again after waiting
        local newDist = (closestBush.model.PrimaryPart.Position - root.Position).Magnitude
        if newDist <= range then -- Check if still within range
             -- Trigger pickup for the bush
            if packets.Pickup and packets.Pickup.send then
                packets.Pickup.send(closestBush.entityid)
            end
        end

    else
        -- No bushes found, look for empty plant boxes instead
        local plantboxes = getpbs(range)
        local closestEmptyBox = nil
        local minDistBox = math.huge

        for _, box in ipairs(plantboxes) do
            if not box.deployable:FindFirstChild("Seed") then
                if box.dist < minDistBox then
                    minDistBox = box.dist
                    closestEmptyBox = box
                end
            end
        end

        if closestEmptyBox then
            -- Found an empty plant box, move towards it
            local targetCFrame = closestEmptyBox.deployable.PrimaryPart.CFrame + Vector3.new(0, 3, 0) -- Adjust offset as needed
            local currentTween = tween(targetCFrame)

             -- Optional: Attempt to plant if close enough after waiting and has seeds
             -- Calculate distance again after waiting
             local newDistToBox = (closestEmptyBox.deployable.PrimaryPart.Position - root.Position).Magnitude
             if newDistToBox <= range then -- Check if still within range
                 -- Check if player has the required seed in inventory (this part might need adjustment based on how inventory works)
                 -- For now, just attempt to plant if close enough and it's an empty box
                 -- A real check would involve verifying the player's inventory contains the specific fruit item ID
                 local itemID = fruittoitemid[currentFruitName] or 94
                 -- Simple attempt to plant (requires player to have the item)
                 if packets.InteractStructure and packets.InteractStructure.send then
                     pcall(function()
                         packets.InteractStructure.send({
                             entityID = closestEmptyBox.entityid,
                             itemID = itemID
                         })
                     end)
                     -- lastPlantTime = tick() -- Uncomment if you want a cooldown per plant action
                 end
             end
        end
    end
end

-- Connect toggles with the improved functions (using Heartbeat for non-stop movement)
local plantBoxConn = nil
Options.tweentoplantbox:OnChanged(function(value)
    if value then
        if plantBoxConn then plantBoxConn:Disconnect() end
        plantBoxConn = runs.Heartbeat:Connect(function()
            local range = Options.tweenrange.Value
            tweenplantbox(range)
        end)
    else
        if plantBoxConn then plantBoxConn:Disconnect() end
        -- Explicitly cancel tween when disabling
        if tweening then
            tweening:Cancel()
            tweening = nil
        end
    end
end)

local bushConn = nil
Options.tweentobush:OnChanged(function(value)
    if value then
        if bushConn then bushConn:Disconnect() end
        bushConn = runs.Heartbeat:Connect(function()
            local range = Options.tweenrange.Value
            -- Note: fruitname is now fetched dynamically inside the loop
            tweenpbs(range)
        end)
    else
        if bushConn then bushConn:Disconnect() end
         -- Explicitly cancel tween when disabling
        if tweening then
            tweening:Cancel()
            tweening = nil
        end
    end
end)

-- Update when speed slider changes (restarts active tweens)
Options.tweenspeed:OnChanged(function(value)
    if Options.tweentoplantbox.Value then
        Options.tweentoplantbox.Value = false
        task.wait(0.05)
        Options.tweentoplantbox.Value = true
    elseif Options.tweentobush.Value then
        Options.tweentobush.Value = false
        task.wait(0.05)
        Options.tweentobush.Value = true
    end
end)

Options.tweenrange:OnChanged(function(value)
    -- Could restart tweens here too if desired
    if Options.tweentoplantbox.Value then
        Options.tweentoplantbox.Value = false
        task.wait(0.05)
        Options.tweentoplantbox.Value = true
    elseif Options.tweentobush.Value then
        Options.tweentobush.Value = false
        task.wait(0.05)
        Options.tweentobush.Value = true
    end
end)

-- IMPROVED: Auto Plant (Faster, Higher Range)
local plantConnection = nil
Options.planttoggle:OnChanged(function(value)
    if value then
        if plantConnection then
            plantConnection:Disconnect()
        end
        local range = tonumber(Options.plantrange.Value) or 30
        local selectedfruit = Options.fruitdropdown.Value
        local itemID = fruittoitemid[selectedfruit] or 94
        plantConnection = runs.Heartbeat:Connect(function() -- Use Heartbeat for speed
            local plantboxes = getpbs(range)
            for _, box in ipairs(plantboxes) do
                if not box.deployable:FindFirstChild("Seed") then
                    plant(box.entityid, itemID)
                    plantedboxes[box.entityid] = true
                end
            end
            -- Plant delay is effectively handled by Heartbeat frequency if needed,
            -- otherwise remove the delay entirely for max speed here.
        end)
    else
        if plantConnection then
            plantConnection:Disconnect()
            plantConnection = nil
        end
    end
end)

task.spawn(function()
    while true do
        if not Options.harvesttoggle.Value then
            task.wait(0.1)
            continue
        end
        if not root then
            task.wait(0.1)
            continue
        end
        local harvestrange = tonumber(Options.harvestrange.Value) or 30
        local selectedfruit = Options.fruitdropdown.Value
        local bushes = getbushes(harvestrange, selectedfruit)
        table.sort(bushes, function(a, b) return a.dist < b.dist end)
        for _, bush in ipairs(bushes) do
            pickup(bush.entityid)
        end
        task.wait(0.1)
    end
end)

-- IMPROVED: Place Structure (Uses Snapping Logic, Handles Single and Grid Placement, Includes Continuous Option, Adds Cursor, Adds Rotation Choice)
local placementConnection = nil
local function placeSingleSnappedBox()
    local player = game.Players.LocalPlayer
    local character = player.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") then
        warn("Cannot place box: no character or HumanoidRootPart")
        return
    end

    local rootPart = character.HumanoidRootPart
    local currentPosition = rootPart.Position
    local camera = workspace.CurrentCamera
    local cursorPos = camera and camera.CFrame.Position or currentPosition -- Fallback to root pos if camera fails

    -- Snap to grid
    local gridSize = 6.05
    local snappedX = math.round(currentPosition.X / gridSize) * gridSize
    local snappedZ = math.round(currentPosition.Z / gridSize) * gridSize
    local snappedPosition = Vector3.new(snappedX, currentPosition.Y, snappedZ)

    -- Raycast down
    local rayParams = RaycastParams.new()
    rayParams.FilterType = Enum.RaycastFilterType.Blacklist
    rayParams.FilterDescendantsInstances = {character}
    rayParams.IgnoreWater = true

    local rayOrigin = Vector3.new(snappedPosition.X, snappedPosition.Y + 100, snappedPosition.Z)
    local rayDirection = Vector3.new(0, -200, 0)
    local rayResult = workspace:Raycast(rayOrigin, rayDirection, rayParams)

    local finalPosition = rayResult and rayResult.Position or snappedPosition

    -- Determine rotation based on dropdown
    local selectedRotation = Options.rotationdropdown.Value
    local rotationCFrame = CFrame.Angles(0, 0, 0) -- 0 degrees
    if selectedRotation == "45 degrees" then
        rotationCFrame = CFrame.Angles(0, math.rad(45), 0) -- 45 degrees around Y
    end
    local finalCFrame = CFrame.new(finalPosition) * rotationCFrame

    -- ✅ CRITICAL: Include 'cursor' field (required by ByteNet) and rotation via CFrame
    if packets.PlaceStructure and packets.PlaceStructure.send then
        packets.PlaceStructure.send({
            buildingName = "Plant Box",
            cframe = finalCFrame, -- ✅ Rotation baked in
            vec = finalPosition,
            isMobile = false,
            cursor = cursorPos  -- 👈 MUST be present and non-nil
        })
        print("Placed plant box at:", finalPosition, "with rotation:", selectedRotation)
    else
        warn("PlaceStructure packet not found or send method missing.")
    end
end

-- Grid placement function (keeps original grid logic but uses snapped positions, adds cursor, adds rotation choice)
local function placeGridOfBoxes(gridSize)
    local player = game.Players.LocalPlayer
    local character = player.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") then
        print("Player character or root part not found for placement.")
        return
    end

    local torso = character.HumanoidRootPart
    local camera = workspace.CurrentCamera
    local cursorPos = camera and camera.CFrame.Position or torso.Position -- Fallback

    -- Determine rotation based on dropdown
    local selectedRotation = Options.rotationdropdown.Value
    local rotationCFrame = CFrame.Angles(0, 0, 0)
    if selectedRotation == "45 degrees" then
        rotationCFrame = CFrame.Angles(0, math.rad(45), 0)
    end

    local startpos = torso.Position - Vector3.new(0, 3, 0) -- Offset down slightly from player
    local spacing = 6.05 -- Approximate spacing between plant boxes (matches snap grid)

    for x = 0, gridSize - 1 do
        for z = 0, gridSize - 1 do
            task.wait(0.3) -- Add a small delay between placement attempts

            -- Calculate grid position
            local gridX = startpos.X + (x * spacing)
            local gridZ = startpos.Z + (z * spacing)
            local gridPos = Vector3.new(gridX, startpos.Y, gridZ)

             -- Snap grid position to the same grid system
            local snappedX = math.round(gridPos.X / spacing) * spacing
            local snappedZ = math.round(gridPos.Z / spacing) * spacing
            local snappedGridPos = Vector3.new(snappedX, gridPos.Y, snappedZ)

            -- Raycast down from the snapped grid position
            local rayParams = RaycastParams.new()
            rayParams.FilterType = Enum.RaycastFilterType.Blacklist
            rayParams.FilterDescendantsInstances = {character}
            rayParams.IgnoreWater = true

            local rayOrigin = Vector3.new(snappedGridPos.X, snappedGridPos.Y + 100, snappedGridPos.Z)
            local rayDirection = Vector3.new(0, -200, 0)

            local rayResult = workspace:Raycast(rayOrigin, rayDirection, rayParams)

            local finalPosition = rayResult and rayResult.Position or snappedGridPos
            local finalCFrame = CFrame.new(finalPosition) * rotationCFrame -- ✅ Rotated CFrame

            -- ✅ CRITICAL: Include 'cursor' field (required by ByteNet) and rotation via CFrame
            if packets.PlaceStructure and packets.PlaceStructure.send then
                packets.PlaceStructure.send({
                    buildingName = "Plant Box",
                    cframe = finalCFrame, -- ✅ Rotation baked in
                    vec = finalPosition,
                    isMobile = false,
                    cursor = cursorPos -- 👈 MUST be present and non-nil
                })
                print("Placed plant box in grid at:", finalPosition, "with rotation:", selectedRotation)
            else
                 warn("PlaceStructure packet not found or send method missing during grid placement.")
                 break -- Stop if packet fails
            end
        end
    end
end

-- Update the placestructure function to handle both grid and single snapped placement
placestructure = function(gridsize)
    if gridsize == 1 then
        -- If gridsize is 1, treat it as a single snapped placement
        placeSingleSnappedBox()
    else
        -- Otherwise, treat it as a grid placement
        placeGridOfBoxes(gridsize)
    end
end

-- Handle the continuous placement toggle
Options.placeplantboxestoggle:OnChanged(function(value)
    if value then
        -- Enable continuous placement
        if placementConnection then
            placementConnection:Disconnect() -- Disconnect any existing connection
        end
        placementConnection = runs.Heartbeat:Connect(function()
            placeSingleSnappedBox() -- Place a box every heartbeat frame
            task.wait(0.1) -- Add a small delay to prevent spamming too quickly
        end)
    else
        -- Disable continuous placement
        if placementConnection then
            placementConnection:Disconnect()
            placementConnection = nil
        end
    end
end)

-- ==================== AUTO REBIRTH SYSTEM (Simplified) ====================
local TS = game:GetService("TeleportService")
local Players = game:GetService("Players")
local plr = Players.LocalPlayer
local rs = game:GetService("ReplicatedStorage")

local PLACE_ID = game.PlaceId
local PRIVATE_SERVER_CODE = "5031d22683fd2446b995b52491d09fae"

local autoRebirthRunning = false

-- ====================== HELPER FUNCTIONS ======================

local function pressBedButton()
    -- Spam bed button / spawn
    for _, gui in ipairs(plr.PlayerGui:GetChildren()) do
        local bedButton = gui:FindFirstChild("BedButton", true)
        if bedButton then
            pcall(function() bedButton.Activated:Fire() end)
            pcall(function() firesignal(bedButton.Activated) end)
        end
    end
    
    pcall(function()
        rs.Events.SpawnFirst:InvokeServer(true)
    end)
end

local function getPlayerLevel()
    local success, GameUtilLocal = pcall(function() return require(rs.Modules.GameUtil) end)
    if success and GameUtilLocal.Data and GameUtilLocal.Data.level then
        return GameUtilLocal.Data.level
    end
    return plr:GetAttribute("Level") or 0
end

local function doRebirth()
    print("🎉 Level 100 reached → Triggering Rebirth")
    
    local success, packetsLocal = pcall(function() return require(rs.Modules.Packets) end)
    if success and packetsLocal.Rebirth then
        pcall(function()
            packetsLocal.Rebirth.send()
            print("✅ Rebirth packet sent")
        end)
    end
    
    task.wait(2)
    pressBedButton()
end

local function resetCharacter()
    local char = plr.Character
    if char and char:FindFirstChild("Humanoid") then
        print("💀 Resetting character...")
        char.Humanoid.Health = 0
    end
end

-- ====================== MAIN LOOP ======================

local function AutoRebirthLoop()
    print("🚀 Auto Rebirth System Started (Level 100 → Rebirth → Reset)")
    
    while autoRebirthRunning do
        task.wait(0.5)
        
        -- Constantly spam bed/spawn when needed
        local spawnGui = plr.PlayerGui:FindFirstChild("SpawnGui")
        if spawnGui and spawnGui.Enabled then
            pressBedButton()
        end
        
        local currentLevel = getPlayerLevel()
        
        -- LEVEL 100 CHECK
        if currentLevel >= 100 then
            print("🎉 LEVEL 100 DETECTED! Current Level: " .. currentLevel)
            
            doRebirth()
            task.wait(3)           -- Wait for rebirth to process
          --  resetCharacter()       -- Reset character as requested
            task.wait(4)           -- Wait after reset
            
            print("🔄 Cycle completed. Starting next run...")
        else
            -- Optional status print (can be removed for less spam)
            if math.random(1, 8) == 1 then
                print("📊 Current Level: " .. currentLevel .. " | Waiting to reach 100...")
            end
        end
        
        task.wait(0.8)
    end
end

-- ====================== START / STOP ======================

local function StartAutoRebirth()
    if autoRebirthRunning then return end
    autoRebirthRunning = true
    print("✅ AUTO REBIRTH ENABLED")
    task.spawn(AutoRebirthLoop)
end

local function StopAutoRebirth()
    autoRebirthRunning = false
    print("⛔ AUTO REBIRTH DISABLED")
end

-- ====================== UI ======================

Tabs.Event:AddSection("Auto Rebirth")
Tabs.Event:AddToggle("AutoRebirthToggle", {
    Title = "Auto Rebirth (Level 100 → Rebirth + Reset)",
    Default = false,
    Callback = function(state)
        if state then
            StartAutoRebirth()
        else
            StopAutoRebirth()
        end
    end
})

print("Auto Rebirth script loaded! Toggle it in the Event tab.")

-- ==================== POTION LOGIC (FULLY INTEGRATED) ====================

local url = "https://raw.githubusercontent.com/nowayden/g/refs/heads/main/fsds.txt"

local orig = game:HttpGet(url)

orig = orig:gsub("local%s+Library%s*=", "Library =")
orig = orig:gsub("local%s+Window%s*=", "Window =")
orig = orig:gsub("local%s+Tabs%s*=", "Tabs =")

local appended = [[

local plr = game.Players.LocalPlayer
local RS = game:GetService("RunService")
local packets = require(game.ReplicatedStorage.Modules.Packets)

-- ========= [ MULTI POTION AUTO CRAFTER + 3 ICE CUBES ] =========
local potionLoop = nil
local isBrewing = false

local ITEM_NAMES = {
    IceCube = "Ice Cube",
    Bloodfruit = "Bloodfruit",
    Strawberry = "Strawberry",
    PricklyPear = "Prickly Pear",
    Crystal = "Crystal",
    Cloudberry = "Cloudberry",
    Adurite = "Adurite",
    FireHide = "Fire Hide",
    Berry = "Berry",
    Magnetite = "Magnetite",
    Iron = "Iron",
    Lemon = "Lemon"
}

-- All potions now start with 3 Ice Cubes
local POTIONS = {
    ["Healing Potion"] = {
        {name = "Ice Cube", amount = 3},
        {name = "Bloodfruit", amount = 5},
        {name = "Strawberry", amount = 2}
    },
    ["Poison Potion"] = {
        {name = "Ice Cube", amount = 3},
        {name = "Prickly Pear", amount = 3},
        {name = "Magnetite", amount = 1}
    },
    ["Swiftness Potion"] = {
        {name = "Ice Cube", amount = 3},
        {name = "Crystal", amount = 1},
        {name = "Cloudberry", amount = 3}
    },
    ["Ice Potion"] = {
        {name = "Ice Cube", amount = 3},
        {name = "Crystal", amount = 1},
        {name = "Ice Cube", amount = 3}  -- extra ice
    },
    ["Harming Potion"] = {
        {name = "Ice Cube", amount = 3},
        {name = "Prickly Pear", amount = 3},
        {name = "Adurite", amount = 3}
    },
    ["Fire Resistance Potion"] = {
        {name = "Ice Cube", amount = 3},
        {name = "Fire Hide", amount = 2}
    },
    ["Strength Potion"] = {
        {name = "Ice Cube", amount = 3},
        {name = "Adurite", amount = 2},
        {name = "Bloodfruit", amount = 2}
    },
    ["Weakness Potion"] = {
        {name = "Ice Cube", amount = 3},
        {name = "Berry", amount = 2},
        {name = "Magnetite", amount = 3}
    },
    ["Haste Potion"] = {
        {name = "Ice Cube", amount = 3},
        {name = "Iron", amount = 2},
        {name = "Lemon", amount = 2}
    }
}

local function getInventorySlot(itemName)
    local pg = plr:FindFirstChild("PlayerGui")
    if not pg then return nil end
    local mg = pg:FindFirstChild("MainGui")
    if not mg then return nil end
    local rp = mg:FindFirstChild("RightPanel")
    if not rp then return nil end
    local inv = rp:FindFirstChild("Inventory")
    if not inv then return nil end
    local list = inv:FindFirstChild("List")
    if not list then return nil end

    for _, slot in ipairs(list:GetChildren()) do
        if slot:IsA("ImageLabel") and slot.Name:find(itemName) then
            return slot.LayoutOrder
        end
    end
    return nil
end

local function dropFromSlot(slot, amount)
    if not slot then return end
    for i = 1, amount do
        pcall(function()
            packets.DropBagItem.send(slot)
        end)
        task.wait(0.07)
    end
end

local function findAllCauldrons()
    local deployables = workspace:FindFirstChild("Deployables")
    if not deployables then return {} end

    local character = plr.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") then return {} end

    local root = character.HumanoidRootPart.Position
    local cauldrons = {}

    for _, obj in ipairs(deployables:GetChildren()) do
        if obj.Name == "Cauldron" then
            local hitbox = obj:FindFirstChild("Hitbox")
            if hitbox then
                local dist = (hitbox.Position - root).Magnitude
                if dist < 30 then
                    table.insert(cauldrons, {cauldron = obj, hitbox = hitbox})
                end
            end
        end
    end
    return cauldrons
end

local function teleportToHitbox(hitbox)
    if not hitbox then return end
    local targetPos = hitbox.Position + Vector3.new(math.random(-2,2)*0.3, 0.8, math.random(-2,2)*0.3)

    local itemsFolder = workspace:FindFirstChild("Items")
    if not itemsFolder then return end

    for _, item in ipairs(itemsFolder:GetChildren()) do
        local name = item.Name or ""
        if name:find("Ice Cube") or name:find("Bloodfruit") or name:find("Strawberry") or 
           name:find("Prickly Pear") or name:find("Crystal") or name:find("Cloudberry") or
           name:find("Adurite") or name:find("Fire Hide") or name:find("Berry") or 
           name:find("Iron") or name:find("Lemon") or name:find("Magnetite") then
            pcall(function()
                if item:IsA("BasePart") then
                    item.Position = targetPos
                elseif item:IsA("Model") and item.PrimaryPart then
                    item:SetPrimaryPartCFrame(CFrame.new(targetPos))
                end
            end)
        end
    end
end

local selectedPotion = "Healing Potion"

local function brewPotions()
    if isBrewing then return end
    isBrewing = true

    local cauldrons = findAllCauldrons()
    if #cauldrons == 0 then
        print("❌ No Cauldrons found")
        isBrewing = false
        return
    end

    local recipe = POTIONS[selectedPotion]
    if not recipe then 
        print("❌ Recipe not found")
        isBrewing = false
        return 
    end

    print("🔄 Brewing " .. selectedPotion .. " in " .. #cauldrons .. " Cauldrons")

    for _, ingredient in ipairs(recipe) do
        local slot = getInventorySlot(ingredient.name)
        if slot then
            for i, c in ipairs(cauldrons) do
                dropFromSlot(slot, ingredient.amount)
                task.wait(0.5)
                teleportToHitbox(c.hitbox)
                task.wait(0.4)
            end
        else
            print("❌ Missing: " .. ingredient.name)
        end
        task.wait(0.8)
    end

    task.wait(2)

    for _, c in ipairs(cauldrons) do
        local entityID = c.cauldron:GetAttribute("EntityID")
        if entityID then
            pcall(function()
                packets.SweepPotion.send(entityID)
            end)
        end
    end

    print("✅ Finished " .. selectedPotion)
    isBrewing = false
end

-- ========= [ UI ] =========
task.wait(0.5)
if not Tabs.Potions then
    Tabs.Potions = Window:AddTab({ Title = "Potions", Icon = "flask" })
end

Tabs.Potions:AddSection("Mass Potion Crafter")
local potionDropdown = Tabs.Potions:AddDropdown("PotionType", {
    Title = "Select Potion",
    Values = {"Healing Potion", "Poison Potion", "Swiftness Potion", "Ice Potion", "Harming Potion", 
              "Fire Resistance Potion", "Strength Potion", "Weakness Potion", "Haste Potion"},
    Default = "Healing Potion",
    Multi = false,
    Callback = function(value)
        selectedPotion = value
    end
})

Tabs.Potions:AddToggle("AutoPotionToggle", {
    Title = "Auto Craft Selected Potion",
    Default = false,
    Callback = function(state)
        if state then
            if potionLoop then potionLoop:Disconnect() end
            potionLoop = RS.Heartbeat:Connect(brewPotions)
            print("✅ Auto Crafting: " .. selectedPotion)
        else
            if potionLoop then potionLoop:Disconnect() end
            potionLoop = nil
            isBrewing = false
            print("❌ Auto Potion Stopped")
        end
    end
})

print("Multi Potion Crafter Loaded (All potions start with 3 Ice Cubes)")
]]

local merged = orig .. appended
local fn, err = loadstring(merged)
if not fn then 
    error("Script loading failed: " .. tostring(err))
else 
    fn() 
end

SaveManager:SetLibrary(Library)
InterfaceManager:SetLibrary(Library)
SaveManager:IgnoreThemeSettings()
SaveManager:SetIgnoreIndexes{}
InterfaceManager:SetFolder("FluentScriptHub")
SaveManager:SetFolder("FluentScriptHub/specific-game")
InterfaceManager:BuildInterfaceSection(Tabs.Settings)
SaveManager:BuildConfigSection(Tabs.Settings)
Window:SelectTab(1)
Library:Notify{
    Title = "neverlose",
    Content = "holy sigma gang",
    Duration = 8
}
SaveManager:LoadAutoloadConfig()
print("done")
